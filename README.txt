In order to run the application, you must install NodeJS first, preferably v20
Afterwards go to where server.js is located in cmd (same path as the file you are reading right now)
Type "node server.js"
Head over to your browser and follow the link "localhost:3000"